spring.application.name=api-paymybuddy

server.port=9000

spring.datasource.url=${MARIADB_HOST:jdbc:mariadb://localhost}:${MARIADB_PORT:3306}/PayMyBuddy
spring.datasource.username=${MARIADB_USERNAME:root}
spring.datasource.password=${MARIADB_PASSWORD:rootroot}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver


# A tester --->   spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.ddl-auto=validate

#spring.jpa.show-sql=true

# ajout de trace + explicites
#spring.jpa.properties.hibernate.format_sql=true  
#logging.level.org.hibernate.SQL=DEBUG 
#logging.level.org.hibernate.type=TRACE 
# fin ajout de trace + explicites

spring.jpa.open-in-view=false
